# MQTT Sensor Logger

## Description
Listens to an MQTT broker for incoming sensor data (temperature, humidity). It parses the payload and logs it directly into a PostgreSQL database for time-series analysis.
